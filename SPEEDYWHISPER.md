# SpeedyWhisper interface pass

The visible app name is SpeedyWhisper. The established `com.audiowhisper.app` bundle identifier, executable/module names, data paths, Python runtime, cached Parakeet v2 model, and existing preferences remain in use. Microphone boosting stays enabled as requested.

Settings/history opens at 620 × 520 points instead of 950 × 700, with a 540 × 420 minimum. Three compact section controls replace the sidebar. History has an inline search field, Copy button, and a menu for the existing delete actions. Its split panes can shrink to fit the smaller window.

The floating recorder keeps its current dimensions and behavior pending the owner's layout preference. The requested review-text → Enter-to-paste flow is the next interaction pass, not part of this branding pass.

The removed welcome screen no longer gates the recording window. Existing history and preference keys remain untouched.
